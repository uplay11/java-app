apiVersion: v1
kind: Service
metadata:
  name: happy-birthday-kush
  namespace: happy-birthday-kush
spec:
  selector:
    app: happy-birthday-kush
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
  type: NodePort
