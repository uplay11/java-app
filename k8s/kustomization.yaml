apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: happy-birthday-kush

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml

images:
  - name: __DOCKERHUB_USERNAME__/happy-birthday-kush
    newTag: latest
